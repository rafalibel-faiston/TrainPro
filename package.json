{
  "name": "trainpro",
  "version": "0.1.0",
  "private": true,
  "description": "TrainPro — deploy único no Railway: o backend serve a API e o build do app web.",
  "scripts": {
    "build": "npm --prefix backend install --no-audit --no-fund && npm --prefix web install --no-audit --no-fund && npm --prefix backend run build && npm --prefix web run build",
    "start": "npm --prefix backend run deploy",
    "seed": "npm --prefix backend run seed",
    "dev:backend": "npm --prefix backend run dev",
    "dev:web": "npm --prefix web run dev"
  }
}
